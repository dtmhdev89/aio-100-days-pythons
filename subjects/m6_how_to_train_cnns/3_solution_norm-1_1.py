# Using Data Normalization in range (-1, 1)
# transform = transforms.Compose(
#     [
#         transforms.ToTensor(),
#         transforms.Normalize(
#             (0.5, 0.5, 0.5), 
#             (0.5, 0.5, 0.5)
#         )
#     ])

# Default init weights in nn.Linear and nn.Conv2d is Kaiming Uniform or He uniform
