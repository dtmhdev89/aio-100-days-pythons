# Useful Concurrent Wrapper

## Usage:
Pass your function into wrapper as a list of pair of functions and arguments for running in multiple process or thread
that based on concurrent built-in library in python
